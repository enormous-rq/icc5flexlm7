/*
 *	Encrypt a buffer buf, of length len, in-place
 */
encrypt(buf, len) char *buf; int len; { }

/*
 *	Decrypt a buffer buf, of length len, in-place
 */
decrypt(buf, len) char *buf; int len; { }
